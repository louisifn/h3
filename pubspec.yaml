name: h3
description: A new flutter plugin project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"


dependencies:
  collection: ^1.14.11
  ffi: ^0.1.3
  flutter:
    sdk: flutter

dev_dependencies:
  test: any
  build_runner:
  build_test:


flutter:
  plugin:
    platforms:
      android:
        package: com.example.h3
        pluginClass: H3Plugin
      ios:
        pluginClass: H3Plugin
